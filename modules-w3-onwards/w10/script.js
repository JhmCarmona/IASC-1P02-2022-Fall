/* 1) we are creating a var that picks the
largest number */

var largestNum = Math.max(10.2, 4.5, 15.6);

console.log(largestNum);

/* 2) we are gonna make another variable to
 round the number given above*/

var roundNum = Math.round(largestNum);

console.log(roundNum);

// 3)  let's create a variable for the square root

var sqrtNum = Math.sqrt(roundNum);

console.log(sqrtNum);

//4) Let's cosole log everything to the page

document.write(sqrtNum)
